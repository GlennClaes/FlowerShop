version: "3.9"

services:
  frontend:
    image: glennclaes123/flowershop-frontend:${IMAGE_TAG}
    container_name: flowershop_frontend
    restart: always
    ports:
      - "80:80"
    networks:
      - flowershop_network

networks:
  flowershop_network:
    driver: bridge