server {
    listen 80;
    server_name localhost; # Luister gewoon lokaal

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html; # GEEN proxy_pass hier!
    }
}